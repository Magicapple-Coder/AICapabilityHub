import { Check, Copy, KeyRound, ShieldCheck } from 'lucide-react'
import { useAuthStore } from '@/stores/auth'
import { useState } from 'react'

export function SettingsPage() { const { userId, userName } = useAuthStore(); const [copied, setCopied] = useState(false); const copy = () => { void navigator.clipboard?.writeText('dev-token-configure-via-VITE_DEV_TOKEN'); setCopied(true); setTimeout(() => setCopied(false), 1600) }; return <section className="mx-auto max-w-[1100px] px-5 pb-32 pt-12 md:px-10 md:pt-20"><p className="mb-5 text-sm text-[#c9f05b]">开发者中心</p><h1 className="text-5xl font-medium tracking-[-.07em] text-[#f4f4ef] md:text-7xl">把工作台<br /><span className="text-white/35">变成你的。</span></h1><div className="mt-16 grid gap-4 md:grid-cols-[.8fr_1.2fr]"><div className="rounded-[1.5rem] bg-[#c9f05b] p-7 text-[#11130f]"><div className="flex h-16 w-16 items-center justify-center rounded-full bg-[#11130f] text-2xl font-medium text-[#c9f05b]">{(userName || 'D').slice(0, 1).toUpperCase()}</div><p className="mt-16 text-xs font-semibold uppercase tracking-[.18em] text-[#11130f]/55">当前账号</p><p className="mt-2 text-3xl font-medium tracking-[-.05em]">{userName || '开发者'}</p><p className="mt-2 text-sm text-[#11130f]/55">ID · {userId || 'dev-user'}</p></div><div className="rounded-[1.5rem] border border-white/10 bg-[#181c17] p-7"><div className="flex items-center gap-3"><ShieldCheck className="text-[#c9f05b]" size={22} /><div><h2 className="text-xl font-medium text-white">访问凭证</h2><p className="mt-1 text-sm text-white/35">本地演示环境使用配置化 JWT。</p></div></div><div className="mt-12 flex items-center justify-between gap-4 border-b border-white/10 pb-4"><div className="flex items-center gap-3 text-sm text-white/55"><KeyRound size={16} /><span className="font-mono">dev-token-••••••••••••</span></div><button onClick={copy} className="flex items-center gap-2 text-xs text-white/45 hover:text-[#c9f05b]">{copied ? <Check size={14} /> : <Copy size={14} />}{copied ? '已复制' : '复制'}</button></div><div className="mt-6 flex items-center gap-2 text-xs text-[#c9f05b]"><span className="h-2 w-2 rounded-full bg-[#c9f05b]" />JWT 配置正常</div></div></div></section> }
